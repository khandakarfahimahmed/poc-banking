import { ZoomAtCoordinatePipe } from "./zoom-at-coordinate.pipe";

describe("ZoomAtCoordinatePipe", () => {
  it("create an instance", () => {
    const pipe = new ZoomAtCoordinatePipe();
    expect(pipe).toBeTruthy();
  });
});
